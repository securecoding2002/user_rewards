spring:
    jpa:
        show-sql: true
    datasource:
        url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
        username: sa
        password: sa
data:
    load:
        user-input: input/users_test.txt
        transaction-input: input/transactions_test.txt
        enabled: true